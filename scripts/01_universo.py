"""FASE 1 — Universo investível.

Importa 'Analise de acoes confiaveis (1).csv' para a tabela `universo`
(métricas em jsonb como justificativa quantitativa), enriquece com a coluna
`apelidos` (nomes populares p/ busca de notícias) e valida cada ticker no
yfinance (série desde 2020-12; emendas MOTV3<-CCRO3 e ISAE4<-TRPL4).
Idempotente: upsert por ticker.
"""
# TODO(FASE 1): implementar após OK da FASE 0.

if __name__ == "__main__":
    raise SystemExit("FASE 1 ainda não implementada — aguardando OK da FASE 0.")
