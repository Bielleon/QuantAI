"""FASE 2 — Notícias históricas do GDELT.

Inspeciona 'noticias_finais.csv' (separador/encoding/colunas), importa para a
tabela `noticias` com dedup por URL (fonte='gdelt'), extrai título legível do
slug da URL e lista as URLs ilegíveis (candidatas ao Cheerio na FASE 3).
Idempotente: insert ignorando duplicatas por url.
"""
# TODO(FASE 2): implementar após OK da FASE 1.

if __name__ == "__main__":
    raise SystemExit("FASE 2 ainda não implementada.")
